name: Deploy Prod

on:
  workflow_dispatch:
    inputs:
      confirm:
        description: Type deploy-prod to confirm production deployment
        required: true

concurrency:
  group: agent-test-prod
  cancel-in-progress: false

permissions:
  contents: read

jobs:
  deploy:
    name: Build and deploy agent-test to prod
    if: ${{ github.event.inputs.confirm == 'deploy-prod' }}
    runs-on:
      - self-hosted
      - k3s
      - prod
    environment: production
    env:
      APP_NAME: agent-test
      CONTAINER_NAME: agent-test
      NAMESPACE: prod
      MANIFEST_DIR: k8s/prod
      IMAGE_TAG: ${{ github.sha }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Runner preflight
        env:
          CHECK_NAMESPACE: prod
        run: ./scripts/check-runner-prereqs.sh

      - name: Test
        run: |
          docker run --rm \
            -v "${{ github.workspace }}:/workspace" \
            -w /workspace \
            python:3.13-slim \
            sh -lc "pip install -e '.[test]' && python -m unittest tests/test_api_contract.py"

      - name: Build local image and deploy to k3s
        run: ./scripts/deploy-local-k3s.sh
